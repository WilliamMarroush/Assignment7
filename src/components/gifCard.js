import React from 'react';
import {Card} from 'react-bootstrap';

const gifCard= (gif)=>{
    console.log(gif.gif);
    const renderGifInfo = () =>(
        <Card.body>
            <img src="">
        </Card.body>
    )
}